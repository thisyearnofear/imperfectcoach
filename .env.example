# Frontend Environment Variables Template
# Copy this to .env.local and fill in your values
# NEVER commit .env.local to git!

# AWS Lambda/API Gateway Endpoints
# Get these URLs after deploying your Lambda functions
VITE_PREMIUM_ANALYSIS_URL=https://YOUR_API_ID.execute-api.eu-north-1.amazonaws.com/analyze-workout
VITE_AGENT_CORE_URL=https://YOUR_API_ID.execute-api.eu-north-1.amazonaws.com/agent-coach
VITE_AGENT_DISCOVERY_URL=https://YOUR_API_ID.execute-api.eu-north-1.amazonaws.com/discovery

# Default Network (optional, defaults to base-mainnet)
# Options: base-mainnet, base-sepolia, avalanche-mainnet, avalanche-fuji, solana-devnet
VITE_DEFAULT_NETWORK=base-mainnet

# Feature Flags (optional, all enabled by default)
# VITE_ENABLE_PREMIUM=true
# VITE_ENABLE_DISCOVERY=true
# VITE_ENABLE_MULTI_NETWORK=true

# Enhanced Agent Economy Feature Flags
# Enable Kestra AI orchestration for advanced analysis synthesis
ENABLE_KESTRA_ORCHESTRATION=true

# Enable Oumi model enhancements for specialized fitness insights
ENABLE_OUMI_ENHANCEMENTS=true

# Enable real x402 blockchain payments (start with false for testing)
ENABLE_REAL_X402_PAYMENTS=false

# Enable agent marketplace for dynamic agent discovery
ENABLE_AGENT_MARKETPLACE=true

# Fallback to legacy system if enhanced features fail
FALLBACK_TO_LEGACY=true

# Kestra AI Configuration
# Required when ENABLE_KESTRA_ORCHESTRATION=true
# KESTRA_API_URL=https://your-kestra-instance.com
# KESTRA_API_KEY=your_kestra_api_key
# KESTRA_WORKSPACE_ID=your_workspace_id

# Oumi Model Training Configuration
# Required when ENABLE_OUMI_ENHANCEMENTS=true
# OUMI_API_KEY=your_oumi_api_key
# OUMI_WORKSPACE_ID=your_oumi_workspace_id

# WalletConnect Project ID (required for wallet connections)
# Get your free project ID from: https://walletconnect.com/
VITE_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id_here

# Supabase (if still using for auth/other features)
# VITE_SUPABASE_URL=your-project-url
# VITE_SUPABASE_ANON_KEY=your-anon-key

# Solana RPC Configuration (CRITICAL for leaderboard data)
# Must be set to avoid rate limiting on public api.devnet.solana.com
# Options:
#   - Helius (fastest): Get free key at https://www.helius.dev/
#   - Alchemy: https://www.alchemy.com/
#   - Magic Eden: https://www.magiceden.io/
# HELIUS_API_KEY or VITE_HELIUS_API_KEY=your_helius_api_key
# VITE_SOLANA_DEVNET_RPC_URL=https://api.devnet.solana.com  (fallback only)
VITE_HELIUS_API_KEY=
