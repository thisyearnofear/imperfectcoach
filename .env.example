# Frontend Environment Variables Template
# Copy this to .env.local and fill in your values
# NEVER commit .env.local to git!

# AWS Lambda/API Gateway Endpoints
# Get these URLs after deploying your Lambda functions
VITE_PREMIUM_ANALYSIS_URL=https://YOUR_API_ID.execute-api.eu-north-1.amazonaws.com/analyze-workout
VITE_AGENT_CORE_URL=https://YOUR_API_ID.execute-api.eu-north-1.amazonaws.com/agent-coach
VITE_AGENT_DISCOVERY_URL=https://YOUR_API_ID.execute-api.eu-north-1.amazonaws.com/discovery

# Default Network (optional, defaults to base-mainnet)
# Options: base-mainnet, base-sepolia, avalanche-mainnet, avalanche-fuji, solana-devnet
VITE_DEFAULT_NETWORK=base-mainnet

# Feature Flags (optional, all enabled by default)
# VITE_ENABLE_PREMIUM=true
# VITE_ENABLE_DISCOVERY=true
# VITE_ENABLE_MULTI_NETWORK=true

# WalletConnect Project ID (required for wallet connections)
# Get your free project ID from: https://walletconnect.com/
VITE_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id_here

# Supabase (if still using for auth/other features)
# VITE_SUPABASE_URL=your-project-url
# VITE_SUPABASE_ANON_KEY=your-anon-key
