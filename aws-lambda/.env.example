# AWS Lambda Environment Variables Template
# Copy this to your AWS Lambda configuration via Console or CLI
# NEVER commit actual credentials to git!

# Amazon Bedrock Model
BEDROCK_MODEL_ID=amazon.nova-lite-v1:0

# Coinbase Developer Platform (CDP) Credentials
# Get these from: https://portal.cdp.coinbase.com
CDP_API_KEY_ID=your-cdp-api-key-id-here
CDP_API_KEY_SECRET=your-cdp-api-key-secret-here
CDP_WALLET_SECRET=your-cdp-wallet-secret-here

# Paymaster URL (from CDP Portal → Paymaster → Configuration)
PAYMASTER_URL=https://api.developer.coinbase.com/rpc/v1/base/YOUR_API_KEY

# Optional: Regular EVM private key (if not using CDP wallet)
# AGENT_PRIVATE_KEY=0x...

# DynamoDB Table Names (auto-detected if using default names)
# WORKOUT_HISTORY_TABLE=ImperfectCoach-WorkoutHistory
# USER_PROFILES_TABLE=ImperfectCoach-UserProfiles
# AGENT_SESSIONS_TABLE=ImperfectCoach-AgentSessions
